# ── Credentials & config (NEVER commit these) ────────────────
config.php
.env
*.env

# ── User-uploaded files (live on server, never commit) ────────
property_images/
uploads/

# ── OS / editor junk ─────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.sublime-workspace

# ── PHP error logs ────────────────────────────────────────────
*.log
error_log
