# ── Credentials & config (NEVER commit these) ────────────────
config.php
.env
*.env

# ── User-uploaded files (large, already on server) ────────────
# images/ is a symlink on the server pointing to ~/property_images/
# which lives outside the git deployment zone — never commit it
images/
uploads/

# ── OS / editor junk ─────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.sublime-workspace

# ── PHP error logs ────────────────────────────────────────────
*.log
error_log
